# services/ai-worker/requirements.txt
# AI Worker dependencies — flat file for Docker builds
# Keep in sync with pyproject.toml

# Kafka consumer (C extension — fastest Python Kafka client)
confluent-kafka==2.4.0

# MinIO S3-compatible storage client
minio==7.2.7

# Redis client (sync for state writes + pub/sub)
redis==5.0.4

# Async HTTP for LLM API calls (primary I/O workload)
httpx==0.27.0

# Graph algorithms — transitive closure for cheating ring deduplication
networkx==3.3

# Environment variable loading
python-dotenv==1.0.1

# Dev / test only
pytest>=8.1.0
pytest-asyncio>=0.23.0
pytest-cov==5.0.0
pytest-mock==3.14.0
